export class PerformanceDto {

    constructor (id, band, stage, timeFrame) {
        this._id = id,
        this._band = band,
        this._stage = stage,
        this._timeFrame = timeFrame
    };

};